const num1 = 31;
const num2 = 2;
const multiply = num1 * num2; // 31 * 2 = 62

// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1; // Ensures it's always > 0

// Define two numbers for modulo operation
const num3 = 24;
const num4 = 5;
const mod = num3 % num4; // 24 % 5 = 4

// Find the maximum number in a set
const max = Math.max(1, 5, 10, 15, 20);
